// Function to separate tokens
TOKEN * creation(TOKEN *list, char *s, int n_toks);
void enqueue_list(TOKEN * list, int n_toks, QE ** lead, QE ** tail);
int verify_functions(TOKEN * list, int n_toks);

